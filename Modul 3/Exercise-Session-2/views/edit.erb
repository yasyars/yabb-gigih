<!doctype html>
<html lang="en">

<head>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
<head>

<h1 class="d-flex justify-content-center">Edit Item</h1>

<form  class="mt-2" method="POST" action='/items/<%= item.id %>'>
  <div class="d-flex justify-content-center mt-5 form-group">

    <input class="form-text mr-2" name="name" type="text" placeholder="Item name" value="<%=item.name%>">
    <input class="form-text mr-2" name ="price" type="number" placeholder="Item price" value="<%=item.price%>">
    <select class="form-text mr-2"  name="category_id" value=<%= item.id%> :selected=<%= item.id%>>
    <% categories.each do |category| %>
      <option
        value = <%= category.id%>
        <% if category.id == item.category.id%>
        selected
        <%end%>
        >
        <%= category.name%>
      </option>
    <% end %>
    </select>
   <input name="_method" type="hidden" value="put">
  </div>
  <div class="d-flex justify-content-center ">
    <button class=" btn btn-primary mt-3" type="submit"> Update item</button>
  </div>
</form>

<a class="d-flex justify-content-center mt-2" href='/'>Back</a>
</html>